export const VAULT_COOKIE = "vault_unlocked";
/** Cookie must match API routes so the browser sends it on /api/documents/* requests */
export const VAULT_COOKIE_PATH = "/api/documents";
export const VAULT_INACTIVITY_SECONDS = 15 * 60; // 15 minutes
